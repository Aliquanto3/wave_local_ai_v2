# Tech debt

What lands here: 🟢 review findings, never merge-blocking.

| Date | Source review | Location | Issue | Status |
| --- | --- | --- | --- | --- |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/timings.py:37` | Only `KeyError` maps to `MissingTimingsError`; a non-numeric timing value raises `TypeError`/`ValueError` as a raw traceback. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/results.py:12` | `append_row`'s `OSError` isn't caught by either CLI, so a bad results path ends the run with a traceback instead of an `error:` line. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/settings.py:27` | `mistral_api_key` lacks `repr=False`, so `Settings`' generated `__repr__` leaks the live API key. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/server.py:130` | `process.wait()` after `kill()` has no timeout, so `stop_server` can block forever on a stuck process. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/gpu.py:27` | One `except Exception` spans both NVML queries, so a failing power-usage read also discards a successful VRAM reading. | open |
| 2026-08-21 | full-branch-review | `tests/test_server.py:89` | The shutdown assertion accepts either branch, so it can't catch a regression that swapped the platform-correct call. | open |
| 2026-08-21 | full-branch-review | `tests/test_quality_cli.py:59` | Patches started via `p.start()` in a dict comprehension can leak on early failure instead of using `monkeypatch`. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/mistral_client.py:24` | Docstring promises coverage of an unparseable response body, but `response.json()`'s `JSONDecodeError` isn't translated. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/quality_cli.py:28-31,85-92` vs `src/wave_local_ai_v2/__init__.py` | Model path, request timeout, and the completion-POST block are duplicated verbatim across both CLIs. | open |
| 2026-08-21 | full-branch-review | `aidd_docs/tasks/2026_08/2026_08_21_runtime-measurement-harness/review.md:51-79` | Adds an extra "Independent re-derivation" section outside the review validator's closed section list. | open |
| 2026-08-21 | full-branch-review | `.secrets.baseline` | Baseline was generated by scanning the venv, so its inventory is almost entirely third-party noise. | closed |
| 2026-08-21 | full-branch-review | `aidd_docs/backlog/stories/deterministic-classification-scoring-proves-quality-table-split.md:3` | Story and its parent epic were committed as `ready`/in-progress despite the story's acceptance being fully implemented. | open |
| 2026-08-21 | full-branch-review | `CLAUDE.md:33-39` | Seven lines of assistant-behavior rules were added on the feature branch, untraceable to either plan's objective. | open |
| 2026-08-21 | full-branch-review | `src/wave_local_ai_v2/__init__.py:25,217` | `LLAMA_CPP_BUILD` is hardcoded into every runtime row instead of read from the resolved binary. | open |
| 2026-08-21 | full-branch-review | `.gitignore:19` | Results directory was gitignored while the acceptance evidence cited a file inside it, breaking independent reproduction. | open |
| 2026-08-21 | full-branch-review | `aidd_docs/tasks/2026_08/2026_08_21-wave-local-ai-v2-benchmark-suite-prd.md` | Filename uses a dash instead of the sibling underscore convention and sits loose in `tasks/` instead of beside the product brief. | open |
| 2026-08-21 | pr1-hardening | `src/wave_local_ai_v2/server.py:191` | `_read_stderr_tail` seeks the shared stderr handle while the child is still alive, risking a garbled tail on the discarded temp log. | open |
| 2026-08-22 | fresh-machine-onboarding | `docs/setup.md:110-113` | `cp` and `copy` share one ```sh block, so a POSIX reader pasting the block gets `copy: command not found` after the copy already succeeded. | open |
| 2026-08-22 | fresh-machine-onboarding | `docs/setup.md:23` | `git clone <this-repo-url>` leaves a placeholder in a copy-pasteable block though the remote is known (`https://github.com/Aliquanto3/wave_local_ai_v2.git`). | open |
| 2026-08-22 | fresh-machine-onboarding | `.gitignore:12-24` | `.coverage` is a generated artifact sitting untracked in the working tree while every other generated path is ignored in the Project section. | open |
| 2026-08-22 | fast-gate-pre-commit-hook | `.pre-commit-config.yaml:25-29` | `detect-secrets` is the only hook without `always_run`, so a deletion-only commit reports `(no files to check)Skipped` and the secret gate silently does not run. | open |
| 2026-08-22 | fast-gate-pre-commit-hook | `.pre-commit-config.yaml:1-2` | No `minimum_pre_commit_version`, though `default_install_hook_types` needs ≥2.18 and the `pre-commit`/`pre-push` stage names need ≥3.2; a globally installed older binary fails obscurely instead of saying so. | open |
| 2026-08-22 | ci-check-suite | `scripts/audit_dependencies.py:87` | Only `database_specific.severity` is read, so a PYSEC-id finding always resolves `UNKNOWN` and blocks regardless of real severity (`PYSEC-2023-228` → `UNKNOWN`, `GHSA-h4gh-qq45-vh27` → `MODERATE`); following OSV `aliases` to a GHSA id would fix it. | open |
| 2026-08-22 | ci-check-suite | `.github/workflows/ci.yml:46-47` | The audit runs on both matrix legs, but `uv export --format pylock.toml` emits the universal set (84/84 packages) and pip-audit's `PyLockSource._collect_from_packages` ignores `marker` entirely, so both legs audit an identical set — the plan's per-leg Decision no longer holds and the duplicate run only doubles the network-failure surface of a merge-blocking step. | open |
| 2026-08-22 | ci-check-suite | `.github/workflows/ci.yml:35-44` | `Coverage summary` carries `if: always()` but `Upload coverage report` does not, so a failing `pytest` yields a summary and no artifact — the case where the artifact matters most. Observed in run 32574709902: with the fast gate already red, `Coverage summary` ran anyway and failed on both legs (no `.coverage` exists yet), adding a second failing step that masks the real cause. | open |
| 2026-08-22 | ci-check-suite | `scripts/audit_dependencies.py:83` | A transient OSV network error escapes as a traceback; the gate still fails closed, but the run reads as a script bug rather than an audit result. | open |
| 2026-08-22 | ci-check-suite | `scripts/audit_dependencies.py:104` | `_as_date` calls `.replace(tzinfo=UTC)` on a naive `datetime` immediately before `.date()`, which discards it — the call is a no-op. | open |
| 2026-08-22 | ci-check-suite | `tests/test_audit_dependencies.py:5-7` | A module-level `sys.path.insert` mutates global state to reach `scripts/`, forcing the import below the statement; `pythonpath = ["scripts"]` in `[tool.pytest.ini_options]` would replace it. | open |
| 2026-08-22 | ci-check-suite | `pyproject.toml:25-26` | `[tool.coverage.run] source` duplicates `--cov=src/wave_local_ai_v2`; phase-1 made it conditional on `--cov` under-scoping, which it does not (443 statements measured either way). | open |
| 2026-08-22 | ci-check-suite | `.github/workflows/ci.yml:12,49` | Neither job sets `timeout-minutes`, so a hung step burns the default 360 minutes on a workflow that blocks every merge. | open |
| 2026-08-22 | ci-check-suite | `.secrets.baseline` | Story 2's "baseline rescoped over tracked files" verification was performed on Windows, where `detect-secrets` silently skipped every file cp1252 could not decode — so the baseline was audited blind. Re-verify it under UTF-8 mode: `uv run python -X utf8 -m detect_secrets scan` must report only allowlisted lines. | open |
| 2026-08-22 | ci-check-suite | `.github/workflows/ci.yml:20,36` | `actions/checkout@v4` and `actions/upload-artifact@v4` are still referenced by mutable major tags on a workflow that gates merges; pin both by commit sha with the version as a trailing comment, as `astral-sh/setup-uv` now is. The tag form is not merely mutable but unreliable: `@v10` failed the first CI run because no such tag existed. | open |
| 2026-08-22 | main-ruleset | `.github/rulesets/main.json:23` | `require_extra_approval_for_unattributed_changes: true` (a server default captured on export) plus `required_approving_review_count: 0` and a solo maintainer who cannot approve their own PR: a commit whose author email is not linked to a GitHub account would demand an approval nobody can give, deadlocking the merge. Latent — the five most recent commits all resolve to `author_login: Aliquanto3`, and the ruleset was created after PR #10 merged. Either set it `false` and say why in `CONTRIBUTING.md`, or document the deadlock symptom. | open |
| 2026-08-22 | main-ruleset | `aidd_docs/backlog/stories/main-refuses-an-unchecked-merge-including-the-maintainers.md:3` | Frontmatter flipped to `status: done` before the review ran and before the branch merged; `aidd_docs/GUIDELINES.md:18` places the single review after the last phase, then the merge. Repeat of the 2026-08-21 `full-branch-review` row on story status. | open |
| 2026-08-22 | published-image-runs-without-clone | `Dockerfile:12-13` | `LLAMA_SERVER_ASSET` hardcodes `llama-b10537-...` independently of `LLAMA_CPP_TAG=b10537`, so the tag string appears twice and bumping one alone yields a 404 instead of a coherent build. Deriving the asset name from the tag (`llama-${LLAMA_CPP_TAG}-bin-ubuntu-x64.tar.gz`) would make the triple move as one. | open |
| 2026-08-22 | main-ruleset | `aidd_docs/tasks/2026_08/2026_08_22_main-ruleset/evidence.md:29-32,47-50` | §2 titles itself "The ruleset was applied from the tracked file" while §3 explains the file carries "the defaults the server filled in" — so the tracked file was reconciled with the POST response afterwards and is not what §2 POSTed. Harmless, but the two sections read as circular; retitle §2 or state the back-fill there. | open |
| 2026-08-22 | release-tag-names-the-code | `.github/workflows/ci.yml:118-121` | The third sha comparison is unreachable: `revision_label` is already asserted equal to `$GITHUB_SHA` at `:108-111`, so once `reported_sha == revision_label` holds, `reported_sha != $GITHUB_SHA` can never fire. The "three-way agreement" is two independent edges; drop the block or move it above the `:114` check. | open |
| 2026-08-22 | release-tag-names-the-code | `src/wave_local_ai_v2/build_info.py:52-56` | The two deliberate degradation paths are untested: the `except (CalledProcessError, OSError)` branch and the `sha or None` collapse of an empty `stdout`. The branches that exist purely to guarantee "explicit null, never fabricated" are the ones with no proof. | open |
| 2026-08-22 | release-tag-names-the-code | `tests/test_build_info.py:16-17,26-31,41-46` | Isolation is inconsistent with the module's own design: the version case patches the module-local `_installed_version` alias precisely to avoid touching the stdlib globally, but the sha cases patch `build_info.shutil` / `build_info.subprocess`, which *are* the stdlib modules, so a global `subprocess.run` stub is live for anything else running in those tests. Import the two callables under private aliases and patch those. | open |
| 2026-08-22 | row-contract-and-suite-gate | `src/wave_local_ai_v2/classification_suite.py:48` | Superseded by the 2026-08-23 row below: the `CONTEXT_LENGTH = server.CONTEXT_SIZE` binding this row described was replaced by a literal when the versioned-roster increment deleted `server.CONTEXT_SIZE`. | closed |
| 2026-08-22 | row-contract-and-suite-gate | `tests/test_row_contract.py:32-57`, `tests/test_results.py:6-31` | The 24-key `COMPLETE_QUALITY_ROW` literal is duplicated across two test files, once bound to `SCHEMA_VERSION` and once hardcoded to `"1"`. Every future contract extension means editing both, and only one of the two will fail if a writer forgets the field. Move it to `tests/conftest.py`. | open |
| 2026-08-22 | row-contract-and-suite-gate | `src/wave_local_ai_v2/quality_cli.py:240` | `classification_suite.STOP_SEQUENCES` is declared and published on every quality row while being sent to neither provider; the field is truthful only because the list is empty today. `phase-3.md:65` asked for exactly this follow-up note. The generation cap is now threaded through both HTTP paths; thread the stop sequences the same way the first time a suite declares a non-empty list. | open |
| 2026-08-22 | row-contract-and-suite-gate | `src/wave_local_ai_v2/suite_gate.py:108,112` | The invalid-provenance-value and missing-`contamination_risk` branches are the only uncovered lines in the two new modules (`suite_gate.py 49 2 96% 107, 112`), though `phase-2.md:85` specifies both as distinct outcomes ("missing key vs. mismatched value"). Add an item with `provenance="scraped"` and one with `provenance` but no `contamination_risk`. | open |
| 2026-08-22 | row-contract-and-suite-gate | `src/wave_local_ai_v2/suite_gate.py:114` | `item["contamination_risk"] != expected_risk` compares by value, so on the arbitrary mappings the gate is documented to accept, `0` passes as `False` and `1` as `True` — looser than the `bool` the row contract publishes. Require `isinstance(..., bool)` before comparing. | open |
| 2026-08-22 | row-and-generation-provenance | `src/wave_local_ai_v2/quality_cli.py:251` | `endpoint = responses[0]["endpoint"]` raises `IndexError` on an empty suite, and `suite_gate` marks an under-sized suite indicative rather than refusing it, so nothing upstream forbids the case. Unreachable today (the suite is a 10-item module constant). Refuse an empty suite in the gate, or return early from `_run_cloud_suite` before indexing. | open |
| 2026-08-22 | row-and-generation-provenance | `src/wave_local_ai_v2/provenance.py:32-33` | `provenance.py` reaches into `build_info._run_git` and `build_info._PACKAGE_DIR` from another module, so the underscore claims a scope the code does not honour; `provenance.commit_sha()` is a bare one-line delegation to `build_info.commit_sha()` on top. The plan mandated the reuse, not the naming. Rename to `run_git` / `PACKAGE_DIR` (package-public) and let sha callers go to `build_info` directly. | open |
| 2026-08-22 | runtime-repetition-protocol | `src/wave_local_ai_v2/__init__.py:263-282` | `run_repetition_set` returns `(warmups, counted)` but `_run` calls it twice with one half zeroed (`count=0`, then `warmup_count=0`) and discards half of each return, because the energy tracker must span the counted repetitions only. The signature is never used as designed. Split into `run_warmups` / `run_counted`, or keep the shape and say in the docstring that the energy window forces two calls. | open |
| 2026-08-22 | runtime-repetition-protocol | `src/wave_local_ai_v2/aggregation.py:16` | `RepetitionResult` is imported at runtime for a type annotation only, so `row_contract` now transitively pulls `repetitions` → `scoring`, `gpu` and `timings` (psutil) into every importer, `quality_cli` included. Move it under `if TYPE_CHECKING:`. | open |
| 2026-08-22 | runtime-repetition-protocol | `src/wave_local_ai_v2/__init__.py:212` | `MissingTimingsError` is unreachable in `main()`'s caught tuple: `parse_timings` is now called only inside `repetitions._run_one`, which converts it to `RepetitionFailure`. Drop it, or keep it with a comment saying it guards a future direct caller. | open |
| 2026-08-22 | runtime-repetition-protocol | `src/wave_local_ai_v2/timings.py:37` | `GenerationFacts.truncated` is parsed and never read or published, so a `truncated: true` completion is invisible on the row and in the raw repetition list. Either record it on `RepetitionResult` beside `stop_type`, or drop the field and say in the docstring why the flag is not consulted. | open |
| 2026-08-22 | runtime-repetition-protocol | `docs/setup.md:210-218` | "roughly 5x a single request's time plus 50s of cooldown" undercounts: a default run issues six requests (one warm-up plus five counted), and the phase-4 live session ran ~113 s against a ~10.5 s request. Say six requests, or "~6x a single request plus 50 s of cooldown". | open |
| 2026-08-22 | runtime-repetition-protocol | `src/wave_local_ai_v2/repetitions.py:67-80` | `seed_pinned: true` cannot be falsified from `runtime.jsonl`: no repetition persists `content` or a content hash, which phase-4's Evidence row concedes, leaving the reproduction claim resting on an out-of-band probe with no artifact in the repo. Record a `content_sha256` per repetition so two runs can be diffed from the store alone. | open |
| 2026-08-22 | runtime-repetition-protocol | `aidd_docs/results/README.md:12-25` | The section does not note that the two curated rows predate `SCHEMA_VERSION` "2" and the repetition protocol, though the file already applies exactly that discipline to `run_id` / `captured_at` at `:54-59`. A reader comparing them field-by-field to a fresh row meets an unexplained shape mismatch. Add the schema bump to the "deliberately absent" section; regeneration itself stays with order 19. | open |
| 2026-08-22 | runtime-repetition-protocol | `CHANGELOG.md:46` | "a `exceed_context_size_error`" — should be "an". | open |
| 2026-08-22 | row-and-generation-provenance | `CHANGELOG.md:32-34` | The `complete_prompt` return-shape entry sits under `### Added` though it changes an existing public surface from a bare string to a structured result; `phase-4.md:46` names `### Changed` as the default and Keep a Changelog puts it there. Move the bullet to the `### Changed` section. | open |
| 2026-08-22 | machine-state-and-ttft-provenance | `src/wave_local_ai_v2/nvml.py:44-48` | The `EventReasons`-over-`ThrottleReasons` rationale comment sits above `clocks_event_reason_names()`, but the call it justifies (`nvmlDeviceGetCurrentClocksEventReasons`) is in `read_clocks_event_reasons()` further down. Move it onto the reading function, or make it a module-level note. | open |
| 2026-08-22 | machine-state-and-ttft-provenance | `tests/test_machine_state.py:70-78` | The CPU-unavailable test stubs nothing, so it opens a real NVML session as a side effect and asserts a platform-specific `psutil` build; it would fail on any Linux box exposing `coretemp`. Stub `pynvml` for isolation and gate the psutil assertion on `hasattr(psutil, "sensors_temperatures")`. | open |
| 2026-08-22 | machine-state-and-ttft-provenance | `docs/setup.md:210-232` | `RUNTIME_WARMUP_COUNT` is in `.env.example` but documented nowhere in `docs/setup.md`, while the other three runtime settings are. Pre-existing, surfaced while documenting `RUNTIME_SPREAD_THRESHOLD`. | open |
| 2026-08-23 | versioned-roster-and-flags-from-roster | `src/wave_local_ai_v2/roster.py:183-220` | `build_flags_from_entry` has no production caller: the launch-flag ordering is written twice (here and `server.py:59-94`), and the second copy exists only for `tests/test_roster.py:184-217`'s strip-and-compare, which reconstructs by hand what `tests/test_launch_byte_identical.py` already asserts directly against `context_input/baseline_qwen36.md`. Delete the helper and assert `server.build_flags` directly, or give it a production caller by having `server.build_flags` compose it. | open |
| 2026-08-23 | versioned-roster-and-flags-from-roster | `src/wave_local_ai_v2/classification_suite.py:48` | `CONTEXT_LENGTH = 32768` is now a bare literal duplicating the shipped roster entry's `server_flags.context_size`, with nothing binding the two. The value is published on every quality row, cloud rows included, so a roster retune silently makes the suite's own declaration false. Add a test asserting the literal equals the shipped entry's `context_size`. | open |
| 2026-08-23 | versioned-roster-and-flags-from-roster | `tests/test_cli.py:34,220-222` | The fake roster entry's `file` is `"fake.gguf"` (no directory), so `row["model_file"] == FAKE_ROSTER[...]["file"]` passes whether `__init__.py:362` publishes `Path(entry.file).name` or the whole relative path — the one field where the roster's `file` shape differs from the published one is unguarded. Give the fake entry a subdirectory and assert the basename. | open |
| 2026-08-23 | versioned-roster-and-flags-from-roster | `src/wave_local_ai_v2/quality_cli.py:47-63` | `LOCAL_SAMPLING`'s comments state the launched sampler values in prose ("server launched with `--temp 1.0`", "`--top-k 20`", "`--top-p 0.95`", "`--presence-penalty 1.5`"). Those are roster data now and go stale the first time a second entry is selected. Reword to name the source rather than restate the values as fixed. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `aidd_docs/backlog/tech-debt.md:67` | The `versioned-roster-and-flags-from-roster` row about the fake roster entry's `file` shape is now moot: `model_file` left the published runtime row with this increment, and `tests/test_cli.py:215` asserts its absence instead. Close the row or restate it against `fiche["flags"]`, where the model path now lives. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `src/wave_local_ai_v2/verdict.py:157,203` | `differing_fields` names blocking fiche fields on a runtime row and `item_id`s on a quality row — one published key, two meanings. The two stores are never merged so nothing breaks today, but a reader written against one shape misreads the other. Give the quality block its own `differing_item_ids` key. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `src/wave_local_ai_v2/__init__.py:257` | `write_fiche` runs before the server launch, so every run that fails afterwards (startup timeout, `RepetitionFailure`) leaves an orphan fiche in the git-tracked registry with no row citing it, surfacing as noise in `git status`. Register the fiche after the measurement succeeds, or have the validator report unreferenced fiches. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `src/wave_local_ai_v2/fiche_validator.py:142` | `argv` is taken as paths with no flag handling, so `wave-local-ai-v2-validate --help` reads a file named `--help`, finds nothing, prints `checked 0 row(s)` and exits 0. Recognise `-h`/`--help` and print the one-line usage. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `src/wave_local_ai_v2/verdict.py:176,182` | `row.get("sampling", {}).get("seed")` guards an absent `sampling` key but not an explicit `"sampling": null`, which raises `AttributeError`. Use `(row.get("sampling") or {}).get("seed")`. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `tests/test_verdict.py:128` | `test_quality_one_differing_label_is_not_reproduced_and_names_the_item` takes a `tmp_path` fixture it never uses — the quality verdict touches no registry. Drop the parameter. | open |
| 2026-08-24 | fiche-hash-reproduction-verdict | `tests/test_cli.py:276-281` | `test_run_reports_reproduced_against_a_matching_reference_row` rebuilds the settings with `replace(..., runtime_reference_path=reference_path)`, but the `stubbed_run` fixture already points that field at the same `tmp_path / "runtime-reference.jsonl"` — dead setup that reads as if it were doing something. Drop the `replace` call. | open |
| 2026-08-27 | energy-emissions-scope-and-cost | `src/wave_local_ai_v2/mistral_client.py:60` | `MistralCompletion.total_tokens` is read, type-guarded and tested, then never consumed: `_cloud_batch_fields` recomputes the total from the two halves (`quality_cli.py:376`). The provider's own figure is published nowhere and cross-checked against nothing. Assert it matches the computed sum, or drop the field. | open |
| 2026-08-27 | energy-emissions-scope-and-cost | `aidd_docs/product/wave-local-ai-v2.md:32` | The risk table still records the TDP-estimate assumption as "Mitigated by mandatory `energy_method` labelling" — a field this increment deleted. Reword to the three per-channel method labels. | open |
| 2026-08-27 | energy-emissions-scope-and-cost | `aidd_docs/backlog/epics/the-pitch-runs-from-a-browser-and-only-with-the-key.md:45,69` | The declared-absent contract and its acceptance line both key the dashboard's energy-withholding rule on `energy_method`, which no row will carry again. Restate against the three per-channel `*_method` fields before that epic is planned. | open |
| 2026-08-27 | energy-emissions-scope-and-cost | `docs/setup.md:304-332` | Section 5 is titled "Energy, emissions and cost configuration" and gives Scope 2 vs Scope 3 a full comparability paragraph, but says nothing about the cost side's own non-comparability (local `cost_total` in EUR, mistral in USD, never converted) — a caveat that lives only in `CHANGELOG.md:118-120`. Add one sentence: read `cost_currency` before comparing two rows' `cost_total`. | open |
| 2026-08-27 | classification-suite-twenty-items-and-bundle-regeneration | `src/wave_local_ai_v2/suite_snapshot.py:66-72` | `sys.exit(0)` after `main()` is dead — falling off `main()` already exits 0 — and `sys` is imported for nothing else. Drop both. | open |
| 2026-08-27 | classification-suite-twenty-items-and-bundle-regeneration | `src/wave_local_ai_v2/suite_snapshot.py:32` | `SUITE_DEFINITIONS_DIR` is a hardcoded relative `Path`, while every other artifact path is a `settings.DEFAULT_*` constant and `fiche_registry.py:4-5` states the convention outright ("never hardcoded in this module"). Add `DEFAULT_SUITE_DEFINITIONS_DIR` to `settings.py` and read it here. | open |
| 2026-08-27 | classification-suite-twenty-items-and-bundle-regeneration | `src/wave_local_ai_v2/scoring.py:170-172` | `by_language[item["language"]]` raises a bare `KeyError` for a language outside `LANGUAGES`; the sibling path in `suite_gate.py:98-101` raises a named `SuiteGateError` for the same condition. Unreachable today (`Literal` type, gate refuses first). Raise the named error, or `setdefault` and drop the unknown bucket with a comment. | open |
| 2026-08-27 | classification-suite-twenty-items-and-bundle-regeneration | `src/wave_local_ai_v2/classification_suite.py:30-36` | The `"2":` version-history comment sits above `SUITE_ID`, separated from the `SUITE_VERSION` line it documents, unlike `row_contract.py:34-37` where the same convention sits directly above its constant. Move it below `SUITE_ID`. | open |
| 2026-08-27 | classification-suite-twenty-items-and-bundle-regeneration | `tests/test_reference_bundle.py:15-22` | The two reference paths are rebuilt from a local `RESULTS_DIR` while the fiche and roster pointers in the same file come from `settings.DEFAULT_*`; `DEFAULT_RUNTIME_REFERENCE_PATH` / `DEFAULT_QUALITY_REFERENCE_PATH` exist and hold the same values. Read all four from `settings`. | open |
| 2026-08-27 | classification-suite-twenty-items-and-bundle-regeneration | `src/wave_local_ai_v2/classification_suite.py:182-188` | Deferred 🟡, not a nit: `account-de-01` is routed `technical` by `mistral-small-2603` in both published runs (and `other-de-01` in one), so Story 20's "the four routing labels stay semantically disjoint in each language" does not hold in German. Recorded in `aidd_docs/results/README.md` as an observed finding rather than edited, because changing a prompt moves `PROMPT_SET_HASH` and invalidates the bundle just published. Reword the item and re-run the bundle together, in the next regeneration. | open |
| 2026-09-05 | google-cloud-subject | `src/wave_local_ai_v2/google_client.py:280` | The probe-404 message interpolates the module-level `CATALOG_URL` instead of the `catalog_url` built from the `model` argument, so a non-default `model` names the wrong catalog endpoint. Latent: no call site passes one. | open |
| 2026-09-05 | google-cloud-subject | `src/wave_local_ai_v2/quality_cli.py:242` | `for provider in ("mistral", "google")` restates the keys of `_CLOUD_PROVIDERS` right below it; the cloud provider set is now spelled out in four places (`_CLOUD_PROVIDERS`, this tuple, `settings.KNOWN_QUALITY_PROVIDERS`, `cost.PRICE_TABLES`), against plan.md's Decision that a fourth provider should slot in without duplication. Iterate the table instead. | open |
| 2026-09-05 | google-cloud-subject | `CLAUDE.md:62-64` | A file explicitly commented "read on demand, not auto-loaded" was added inside the `<aidd_project_memory>` auto-load block, duplicating the "Load `aidd_docs/memory/external/*` when the user asks" rule two lines below; a memory-hook refresh drops the manual line. | open |
| 2026-09-05 | google-cloud-subject | `tests/test_quality_cli.py:1046` | `from wave_local_ai_v2.row_contract import REQUIRED_FIELDS` sits inside a test body while every other import in the file is module-level. | open |
| 2026-09-05 | google-cloud-subject | `src/wave_local_ai_v2/quality_cli.py:255,284` | `_mistral_batch`/`_google_batch` return a positional 6-tuple annotated inline and unpacked over six lines at the call site; position is the only thing binding `sampling` to `batch_fields`. A small `TypedDict`/`NamedTuple` would name them. | open |
| 2026-09-05 | rate-limited-run-persists-resumes-never-re-pays | `src/wave_local_ai_v2/google_client.py:167-171,344-348` | Each 429/503 branch calls `_retry_hint(response)` for the message and `_parse_retry_delay_s(response)` for the attribute, and the former delegates to the latter, so the error body is JSON-parsed twice per failure and one value is derived along two paths. Parse once into a local, format it inline, pass it to both. | open |
| 2026-09-05 | rate-limited-run-persists-resumes-never-re-pays | `src/wave_local_ai_v2/settings.py:72` | `CLOUD_RETRY_MAX_ATTEMPTS` ("at least one attempt must be allowed") is passed to `RetryBudget(max_retries=...)`, so the configured number is retries beyond the first attempt, not attempts: `=1` yields two calls. The name comes from the plan, so the value is right and the label is not; `cli.md` now says which it is. Rename to `CLOUD_RETRY_MAX_RETRIES` in a schema-free increment. | open |
| 2026-09-05 | rate-limited-run-persists-resumes-never-re-pays | `src/wave_local_ai_v2/quality_cli.py:673-684` | `context_retries` is assigned only on `call_with_retry`'s success path, so a `check_context_fits` that retries two 429s and then raises `ContextWindowExceededError` publishes `retries: 0` on the refusal row — the one row shape where the story's "records how many retries each call took" is silently wrong. Count through a closure or re-raise with the attempt count. | open |
| 2026-09-05 | rate-limited-run-persists-resumes-never-re-pays | `aidd_docs/results/quality-reference.jsonl`, `runtime-reference.jsonl` | The published bundle sits at `schema_version` `"7"` while the code writes `"8"`: its rows are short `retries`/`resumed`, and back-filling them was refused as evidence falsification (`aidd_docs/results/README.md`, "The published bundle is one schema behind the code"). Regenerate on Story 19's protocol — two runtime runs in a quiet thermal window, two quality runs, the validator proof, this README's tables rebuilt — then move `tests/test_reference_bundle.py`'s `PUBLISHED_BUNDLE_SCHEMA_VERSION`. Bench-time work, not a code change. | open |
