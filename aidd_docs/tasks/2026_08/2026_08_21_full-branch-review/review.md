# Review: Full branch (feat/runtime-measurement-harness vs main) — PR #1

- **Verdict**: changes-requested
- **Diff**: `e4c06b6...e1f97bb` (`main...feat/runtime-measurement-harness`, committed state)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_21
- **Findings**: 1 critical, 12 warning, 16 minor

## Phases

<!-- Two plans on this branch: Runtime measurement harness (Increment 1) and Deterministic classification scoring. -->

### Plan: Runtime measurement harness (Increment 1) — Phase 1 — Fiche and results store

- [x] `load_settings()` populated from env; unset `LLAMA_SERVER_PATH` raises before any spawn — `settings.py:30-63`, `tests/test_settings.py:19-79`
- [ ] `capture_fiche()` returns all documented keys and degrades to `None` when NVML raises — key-set assertion is real (`tests/test_hardware.py:7-14`), but the degradation test monkeypatches `hardware._capture_gpu_fields` itself (`tests/test_hardware.py:22`), so `hardware.py:47-58`'s `except` — the code the criterion targets — never executes
- [x] `append_row`/`read_rows` round-trip on a tmp path, parent dirs created — `results.py:10-22`, `tests/test_results.py:6-26`

### Plan: Runtime measurement harness (Increment 1) — Phase 2 — llama-server process lifecycle

- [x] `build_flags` matches the baseline flag list exactly, by exact list comparison — `server.py:45-79`, `tests/test_server.py:10-45`
- [x] `start_server` returns only after a ready `/health`; a dead process raises without sleeping through the timeout — `server.py:96-115`, `tests/test_server.py:48-80` (`mock_sleep.assert_not_called()`)
- [ ] `running_server` terminates, and kills if still alive, on **both** normal exit and exception — source implements both (`server.py:126-130`, `:139-142`), but only the exception path is tested and with `stop_server` itself mocked (`tests/test_server.py:102-114`); no normal-exit test, and the `TimeoutExpired` -> `kill()` fallback is uncovered

### Plan: Runtime measurement harness (Increment 1) — Phase 3 — Metrics collection

- [x] `parse_timings` returns the three fields; a missing `timings` raises `MissingTimingsError`, not `KeyError` — `timings.py:27-42`, `tests/test_timings.py:22-37`
- [x] `read_gpu_stats` returns documented keys, degrades to `None` on an NVML raise, session scoped per call — `gpu.py:15-28`, `nvml.py:16-25`, `tests/test_gpu.py:20-38`, `tests/test_nvml.py:20-33`
- [x] `measure_energy` tags `measured_nvml`/`estimated_tdp`/`unavailable`; tracker-init failure does not raise — `energy.py:21-43`, `tests/test_energy.py:15-44`

### Plan: Runtime measurement harness (Increment 1) — Phase 4 — CLI wiring (end to end)

- [ ] The stubbed `requests.post` carries the fixed prompt and fixed `max_tokens` exactly once per `main()` — the code does send it once (`__init__.py:194-205`), but no test inspects the stub: `tests/test_cli.py:46-53` patches `requests.post` and never asserts `call_args`/`call_count`; a payload-key or duplicate-call regression would pass
- [x] A stubbed end-to-end run appends exactly one row with every fiche field, the flag list and `energy_method`; readiness and mid-run failures append zero rows and still shut down — `__init__.py:167-229`, `tests/test_cli.py:70-83,86-115,118-145`
- [x] A real run's `gen_tok_per_s` is within +/-1.5 of 26 and `prompt_tok_per_s` meets the re-scoped bar — re-derived from `aidd_docs/results/runtime.jsonl`: 26.046/25.484 `gen_tok_per_s`, 255.93/259.25 `prompt_tok_per_s`; re-scope justified in `plan.md` Decisions and `debug-prefill-gap.md`
- [ ] (task 1.2) Independent wall-clock TTFT cross-check — not implemented; `__init__.py:207-209` records `wall_clock_s`, not TTFT. Phase-4's acceptance table grades task 1 solely on the fixed prompt/`max_tokens` request, so this is a task step, not a graded criterion — tagged `not-applicable`, revert documented at `__init__.py:178-193`

### Plan: Deterministic classification scoring — Phase 1 — Classification task suite and deterministic scorer

- [x] Suite has >= 8 items (10), every `expected_label` in `LABELS`, no I/O on import — `classification_suite.py:19-96`, `tests/test_classification_suite.py:4-23`
- [x] `normalize_label` matches clean/noisy/mixed-case, returns `None` and never raises with no match — `scoring.py:20-30`, `tests/test_scoring.py:5-21`
- [x] `score_item` correct only on exact match, `False` without raising when unparseable — `scoring.py:42-50`, `tests/test_scoring.py:24-48`
- [x] `score_suite([]) == 0.0`; a mixed list returns the exact fraction — `scoring.py:53-61`, `tests/test_scoring.py:51-63`

### Plan: Deterministic classification scoring — Phase 2 — Cloud model client (Mistral)

- [x] `complete_prompt` returns the exact `content`, with correct URL/headers/body — `mistral_client.py:27-52`, `tests/test_mistral_client.py:15-27`
- [x] Non-200 raises `MistralRequestError` naming the status code — `mistral_client.py:39-43`, `tests/test_mistral_client.py:30-38`
- [x] A 200 body missing `choices` raises `MistralRequestError`, not a raw `KeyError`/`IndexError` — `mistral_client.py:46-51`, `tests/test_mistral_client.py:41-51`
- [x] No test makes a live network call — all three stub `requests.post` — `tests/test_mistral_client.py:16,32,43`

### Plan: Deterministic classification scoring — Phase 3 — Quality results store and settings

- [x] `load_settings()` returns `quality_results_path`/`mistral_api_key` from env, falls back without raising — `settings.py:12,26-27,42-45`, `tests/test_settings.py:19-56`
- [x] Existing three-field `Settings(...)` call sites still pass unmodified — `settings.py:26-27`, `tests/test_cli.py:30-34,94-98,128-132`
- [x] `.env.example` lists `QUALITY_RESULTS_PATH` with the same default used in code — `.env.example:6`, `settings.py:12`

### Plan: Deterministic classification scoring — Phase 4 — CLI wiring (end to end, both models)

- [x] Server started exactly once for the suite; no local row carries a fiche/timings/energy field — `quality_cli.py:85-108`, `tests/test_quality_cli.py:78-84,87-93`
- [x] One Mistral call per item with that item's prompt; empty key raises `SettingsError` before any HTTP call — `quality_cli.py:54-58,111-115`, `tests/test_quality_cli.py:109-119,122-161`
- [x] `2 * len(suite)` rows, `prompt` verbatim, `suite_accuracy` equal within a model — `quality_cli.py:121-139`, `tests/test_quality_cli.py:69-75,96-106`
- [x] No quality row carries a runtime-only field, and vice versa — `tests/test_quality_cli.py:11-19,87-93`, `__init__.py:215-228`
- [x] `wave-local-ai-v2-quality` registered; boundary errors print to stderr and exit 1 — `pyproject.toml:24`, `quality_cli.py:38-49`, `tests/test_quality_cli.py:198-210`

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | fit | Quality:4 | `src/wave_local_ai_v2/server.py:30-34`, `src/wave_local_ai_v2/mistral_client.py:35` | Quality scores are not reproducible. `quality_cli.py:82` reuses `server.build_flags`, the runtime benchmark's sampling set: `--temp 1.0 --top-p 0.95 --top-k 20 --presence-penalty 1.5`, and no `--seed` appears anywhere in `src/`. The Mistral call sends neither `temperature` nor `random_seed`. `architecture.md:32` defines quality scores as "reproducible (model + prompt + seed)" and the plan objective promises "a deterministic quality score"; only the scorer is deterministic, the score itself varies per run. | Give the quality run its own flag set: `--seed <fixed>` and `--temp 0`, separate from the runtime benchmark's. Send `temperature: 0` and `random_seed` to Mistral. Record both in every quality row. |
| 🟡 | code | Quality:2 | `src/wave_local_ai_v2/mistral_client.py:47` | `content: str = response_json[...]["content"]` annotates `str` and never checks it. Mistral returns `content: null` on tool-call/refusal finish reasons; the value reaches `normalize_label`, whose `.lower()` raises `AttributeError` outside `main()`'s except tuple — a raw traceback. The local path received exactly this guard in `e1f97bb` (`quality_cli.py:100`); the cloud path did not. | Mirror the local guard: `if not isinstance(content, str): raise MistralRequestError(...)` before returning. |
| 🟡 | functional | Runtime:4 | `tests/test_cli.py` | Phase-4 criterion 1 requires showing the stubbed `requests.post` carries the fixed prompt and `max_tokens` exactly once. `post` appears only as a patch target (lines 46, 106); no `call_args`/`call_count` assertion exists in the file. The same fixture also leaves `measure_energy` unpatched despite its docstring claiming otherwise, so real CodeCarbon runs: measured 5.39s + 2.95s for two pure-mock tests. | Assert `post.call_count == 1` and `call_args.kwargs["json"] == {"prompt": FIXED_PROMPT, "n_predict": FIXED_MAX_TOKENS}`; patch `measure_energy` in the fixture and assert the concrete `energy_method`. |
| 🟡 | functional | Runtime:1 | `tests/test_hardware.py:22` | Phase-1 criterion 2 requires that stubbing NVML to raise still yields `gpu_name is None` with nothing propagating. The test monkeypatches `hardware._capture_gpu_fields` to return `(None, None, None)` — stubbing out the exact code under test — so `hardware.py:47-58`'s `except` never runs. `tests/test_gpu.py:33` already demonstrates the raise-stub pattern that was available. | Install a fake `pynvml` in `sys.modules` whose `nvmlInit`/`nvmlDeviceGetName` raises, as `test_gpu.py` does, and assert the three GPU fields come back `None`. |
| 🟡 | functional | Runtime:2 | `tests/test_server.py` | Phase-2 criterion 3 requires terminate-and-kill-if-alive on both normal exit and exception. Only the exception path is tested, and it mocks `stop_server` itself (lines 102-114). No test covers `running_server` normal exit, and the `TimeoutExpired` -> `kill()` fallback at `server.py:126-130` is entirely uncovered. | Add a normal-exit test asserting `stop_server` is called, and a `stop_server` test where `process.wait` raises `TimeoutExpired` first, asserting `kill()` then a second `wait()`. |
| 🟡 | code | Runtime:2 | `src/wave_local_ai_v2/server.py:104` | The readiness poll hits `/health` without confirming the responder is the process just spawned. If a stale llama-server already holds port 8080, the new process fails to bind and dies, yet the first poll returns 200 from the old one: `start_server` returns a doomed handle and every metric is attributed to the wrong server. | Fail fast if the port is already listening before `Popen`, or gate the poll on `process.poll() is None` plus a marker unique to the spawned process. |
| 🟡 | code | Runtime:2 | `src/wave_local_ai_v2/server.py:84` | `with tempfile.TemporaryFile()` closes the parent handle when `start_server` returns (line 106). The child keeps writing stderr to a file nobody can read, so a mid-run crash surfaces as a bare `ConnectionError` with no diagnostics, and the delete-on-close log grows unbounded for the session. | Own the stderr sink in `running_server` for the process lifetime, or write a named file under the results dir so error paths can tail it. |
| 🟡 | code | Runtime:3 | `src/wave_local_ai_v2/energy.py:34` | Tracker construction and `start()` are guarded (line 28) but `tracker.stop()` in the `finally` is not. A CodeCarbon teardown failure propagates as an unhandled type, destroying an already-completed measurement, and silently replaces any exception `fn()` raised. | Wrap `tracker.stop()` in try/except, degrade to `energy_method="unavailable"`, and preserve both `result` and any original exception. |
| 🟡 | code | Quality:4 | `src/wave_local_ai_v2/quality_cli.py:60` | Local completions are held in memory across the whole cloud suite; nothing persists until line 63. Any cloud failure after the multi-minute local run — 429, transient network, malformed body — discards all local work with zero rows written. The upfront key check covers only the missing-key case, not runtime failures. | Score and append the local rows immediately after `_run_local_suite`, before the cloud suite starts. |
| 🟡 | code | Runtime:4 | `src/wave_local_ai_v2/__init__.py:213` | `read_process_rss(process.pid)` can raise `psutil.NoSuchProcess` (server exits between the completion response and this call) or `AccessDenied`; neither is in `main()`'s except tuple (lines 157-162), so the run dies with a traceback after the measurement already succeeded. | Catch psutil errors inside `read_process_rss` and return `None`, or add `psutil.Error` to `main()`'s handled tuple. |
| 🟡 | rot | - | `aidd_docs/memory/cli.md:7`, `aidd_docs/memory/codebase-map.md:16-24` | Project memory, auto-loaded every session per `CLAUDE.md`, is now wrong: `cli.md` lists one command "(implementation in progress)" while this branch ships `wave-local-ai-v2-quality`; `codebase-map.md` names `__init__.py:main` as the only entry point and omits `tests/`, `aidd_docs/results/`, `aidd_docs/backlog/`. The branch's own `audit-and-plan.md:58-60` documents the drift and leaves it unfixed. | Update `cli.md` with the second entry point and drop "in progress"; add the tests, results and backlog areas plus both entry points to `codebase-map.md`. |
| 🟡 | fit | - | `src/wave_local_ai_v2/__init__.py:214-227`, `src/wave_local_ai_v2/quality_cli.py:129-139` | Neither append-only store's rows carry a timestamp or run id, so two runs of the same model are indistinguishable in `runtime.jsonl`/`quality.jsonl` and no row can be attributed to a session. The PRD requires rows "defensible under a client engineer's scrutiny"; satisfied in letter, not in spirit. | Add `run_id` and `captured_at` (UTC ISO-8601) to both row builders. No schema merge; the two tables stay disjoint. |
| 🟡 | conform | - | `CLAUDE.md:33-39` vs `CLAUDE.md:16` | The two new rules contradict the existing Communication rules without the scope/priority `CLAUDE.md:31` itself demands: "No preamble or recap" and "Skip suggestion menus" versus "recommend the most frugal model + effort" at the start of each task and "Recommend the next AIDD skill" with prompt content. | Add an explicit carve-out on the Communication bullet, or fold the new rules under it with stated priority. |
| 🟢 | code | Runtime:3 | `src/wave_local_ai_v2/timings.py:37` | Only `KeyError` maps to `MissingTimingsError`. A `"prompt_ms": null` or non-numeric value makes `float()` raise `TypeError`/`ValueError`, escaping `main()` as a traceback — the same shape-drift failure the module docstring says must be reported clearly. | Add `TypeError, ValueError` to the caught tuple, reusing the `MissingTimingsError` message. |
| 🟢 | code | Runtime:1 | `src/wave_local_ai_v2/results.py:12` | `append_row` can raise `OSError` (results path on a read-only or absent drive, disk full). Neither `__init__.py:157` nor `quality_cli.py:41` catches it, so a misconfigured `RUNTIME_RESULTS_PATH`/`QUALITY_RESULTS_PATH` ends the run with a traceback instead of the `error:` line. | Add `OSError` to both except tuples, or create and validate the results parent directory in `load_settings`. |
| 🟢 | code | Runtime:1 | `src/wave_local_ai_v2/settings.py:27` | `mistral_api_key` is a plain dataclass field, so the generated `__repr__` prints the live key. Any future log line, assertion diff, or traceback frame rendering a `Settings` leaks the credential into a terminal or CI log. | Declare it `field(default="", repr=False)`. |
| 🟢 | code | Runtime:2 | `src/wave_local_ai_v2/server.py:130` | `process.wait()` after `kill()` has no timeout. A process stuck in an uninterruptible driver call — realistic for a GPU-bound process — makes `stop_server`, and so `running_server`'s `finally`, block forever with no diagnostic. | Bound it with `wait(timeout=SHUTDOWN_GRACE_S)` and warn or raise if it still has not exited. |
| 🟢 | code | Runtime:3 | `src/wave_local_ai_v2/gpu.py:27` | One `except Exception` spans both queries, so a failing `nvmlDeviceGetPowerUsage` — unsupported on several consumer parts — also discards the successful VRAM reading, writing `vram_used_mib: null` where a real value existed. | Wrap each query separately so the fields degrade independently. |
| 🟢 | code | - | `tests/test_server.py:89` | `assert fake_process.send_signal.called or fake_process.terminate.called` passes on either branch, so it never checks the platform-correct shutdown path and would not catch a regression that swapped them. | Parametrize on `sys.platform` and assert the specific call. |
| 🟢 | code | - | `tests/test_quality_cli.py:59` | Both fixtures start patches via `p.start()` in a dict comprehension and stop them after the yield. A raise inside the comprehension or an early `p.stop()` leaks the remaining patches into the rest of the session. Both also request `monkeypatch` and never use it. | Use the already-injected `monkeypatch.setattr`, or an `ExitStack`, so cleanup registers per patch as it is applied. |
| 🟢 | standards | Quality:2 | `src/wave_local_ai_v2/mistral_client.py:24` | `MistralRequestError`'s docstring promises it covers "an unparseable response body", but line 45's `response.json()` raises `requests.exceptions.JSONDecodeError` untranslated — the promise holds only for a well-formed body of the wrong shape. | Wrap `response.json()` in the same translation, or narrow the docstring to "unexpected response shape". |
| 🟢 | rot | Quality:4 | `src/wave_local_ai_v2/quality_cli.py:28-31,85-92` vs `src/wave_local_ai_v2/__init__.py:26,151,196-202` | The model relative path, `REQUEST_TIMEOUT_S = 300`, and the `/completion` POST plus `raise_for_status` block are duplicated verbatim across the two CLIs. A model or endpoint change must be made twice and the two can silently diverge. This is distinct from the accepted separate-entry-point decision. | Extract the model constant and a `post_completion(prompt, n_predict)` helper into `server.py`; call it from both CLIs. |
| 🟢 | conform | - | `aidd_docs/tasks/2026_08/2026_08_21_runtime-measurement-harness/review.md:51-79` | Adds a fourth section, `### Independent re-derivation`, plus roughly 400 words of prose. The review skill's `review-validator.yml` declares a closed list: "review.md has exactly these sections. Any other is invalid: remove it, never add or rename" (`Phases`, `Findings`, `Verification`). | Fold the evidence into `Verification` or a `Findings` row and delete the extra heading. |
| 🟢 | rot | - | `.secrets.baseline` | The diff is a legitimate UTF-16 -> UTF-8 re-encode that unblocks `detect-secrets-hook`, and the parsed content matches `main`'s. But 35 of its 36 scanned files are `.venv\Lib\site-packages\...`: the baseline was generated by scanning the virtualenv, so the gate's inventory is almost entirely third-party noise plus one `.env` entry. | Regenerate with the venv excluded (`detect-secrets scan --exclude-files '^\.venv/'`) so the baseline reflects tracked files only. |
| 🟢 | fit | Quality:1 | `aidd_docs/backlog/stories/deterministic-classification-scoring-proves-quality-table-split.md:3` | The story and its parent epic are committed with `status: ready` although the story's acceptance is fully implemented here and its plan is marked `status: implemented`. Backlog state contradicts shipped code from the moment it lands. | Set the story to its done state and the epic to in-progress in the branch that ships it. |
| 🟢 | fit | - | `CLAUDE.md:33-39` | Seven lines of assistant-behavior rules trace to neither plan's `objective:` and to no acceptance criterion, riding on a branch named `feat/runtime-measurement-harness`. Contradicts `CLAUDE.md:22` "Surgical changes... touch only what the task needs". | Carry as its own `docs(claude):` branch and PR so the feature branch stays traceable to its plans. |
| 🟢 | fit | Runtime:1 | `src/wave_local_ai_v2/__init__.py:25,217` | `LLAMA_CPP_BUILD = "b10537"` is hardcoded into every runtime row, but the binary is resolved at runtime from `LLAMA_SERVER_PATH`. A different binary silently mislabels the fiche, and `architecture.md` makes the build part of what makes the number meaningful. | Read the build from `llama-server --version` at launch; keep the constant as a fallback only. |
| 🟢 | fit | Runtime:4 | `.gitignore:19` | The same diff gitignores `aidd_docs/results/` while the branch's only evidence for its headline criterion (tok/s within +/-1.5) is "rows 3-4 of `aidd_docs/results/runtime.jsonl`" — a file nobody else can see. The PRD goal is that a client's engineer can independently reproduce. | Commit the reference rows, or paste the exact numbers into the plan or review doc, so the acceptance evidence survives outside this machine. |
| 🟢 | rot | - | `aidd_docs/tasks/2026_08/2026_08_21-wave-local-ai-v2-benchmark-suite-prd.md` | The filename uses a dash after the date while every sibling in `tasks/2026_08/` uses an underscore, and a PRD sits as a loose file in `tasks/` rather than beside the product brief. | Rename to the underscore form and move next to `aidd_docs/product/`; update the two references in the epic. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 86% (25/29) |
| Files checked | `settings.py`, `hardware.py`, `results.py`, `server.py`, `gpu.py`, `energy.py`, `timings.py`, `nvml.py`, `__init__.py`, `classification_suite.py`, `scoring.py`, `mistral_client.py`, `quality_cli.py`, all 15 `tests/test_*.py`, `pyproject.toml`, `.env.example`, `.gitignore`, `.secrets.baseline`, `CLAUDE.md`, all 8 `aidd_docs/memory/*.md`, both plans' `plan.md` and `phase-1..4.md`, `debug-prefill-gap.md`, `audit-and-plan.md`, `aidd_docs/results/runtime.jsonl`, `uv.lock` |
| Unchecked     | Runtime:1 criterion 2 (NVML-raise degradation stubs out the code under test) — fix; Runtime:2 criterion 3 (normal-exit shutdown and kill fallback uncovered) — fix; Runtime:4 criterion 1 (post payload and call count never asserted, energy unstubbed) — fix; Runtime:4 task 1.2 (independent wall-clock TTFT cross-check) — not-applicable, task step only, revert documented at `__init__.py:178-193` |
| Unplanned     | `nvml.py` + `tests/test_nvml.py` (shared NVML session helper, behavior-preserving refactor 597596f, in no phase's projection); `LocalCompletionError` and the non-`str` content guard (`quality_cli.py:34-36,94-105`, commits d6f2990/e1f97bb, beyond phase-4's specified exception tuple); the runtime row's extra `prompt`, `max_tokens`, `wall_clock_s` fields (`__init__.py:221-223`, `wall_clock_s` being residue of the reverted TTFT cross-check); `pyproject.toml:18-20` mypy override for `pynvml`; `CLAUDE.md:33-39`; `.gitignore` entry for `aidd_docs/results/`; the `.secrets.baseline` re-encode; `uv.lock`; and the documentation-only additions (PRD, epics, stories, prior `review.md` files, `audit-and-plan.md`, `debug-prefill-gap.md`). Gate state at `e1f97bb`: `ruff check`, `ruff format --check`, `mypy src/`, `detect-secrets-hook` and `pytest` (58 passed) all exit 0. This report replaces the prior full-branch review in this folder, whose `changes-requested` verdict rested on two 🟡 findings since fixed in `83fd0fb`/`d6f2990`. |
