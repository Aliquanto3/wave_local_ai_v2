# Review: A versioned roster pins every model, and flags/build come from it

- **Verdict**: approve
- **Diff**: `21e8f7b...working tree` (tracked modifications + 6 untracked files)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_23
- **Findings**: 0 critical, 5 warning (all fixed in-branch), 4 minor (filed to `aidd_docs/backlog/tech-debt.md`)

## Phases

### Phase 1 — Roster file and validation

- [x] `models.json` parses as valid JSON; `architecture.kind == "moe"`, `expert_count == 40`; `validated_host == {n_cpu_moe: 37, threads: 8, fiche_summary: <str>}` — `aidd_docs/roster/models.json:1-37`; asserted at `tests/test_roster.py:220-238`
- [x] The entry's `repo`/`revision`/`file`/`quant`/`sha256` match `docs/setup.md` step 3 **verbatim** — fixed: the subdirectory layout is what the `--local-dir` download step produces and what the roster pins, so `docs/setup.md:160-163` now lists the repo-relative name and the `SLM_MODELS_DIR`-relative path as two distinct bullets, and `:172-178` narrows its "verbatim" claim to repo/revision/checksum plus that path — `aidd_docs/roster/models.json:7`; asserted at `tests/test_roster.py:112`
- [x] `load_roster` returns a valid roster and raises `RosterError` on a checksum-less entry; `resolve_entry` raises on an unknown id; `validate_host_fit` refuses a dense entry with any `n_cpu_moe`, refuses an MoE entry above `expert_count`, passes at or below it — `src/wave_local_ai_v2/roster.py:73-180`; `tests/test_roster.py:92-178` (10 tests, all passing)
- [x] Runtime and quality rows both carry `roster_entry_id`/`roster_version`; `validate_row` raises `RowContractError` when either is absent; full suite green — `src/wave_local_ai_v2/row_contract.py:34-36,102-104`, `__init__.py:354-355`, `quality_cli.py:319-320`; `tests/test_row_contract.py:145-160`, `tests/test_cli.py:219-220`, `tests/test_quality_cli.py:156-157`; `uv run pytest` → 264 passed, 96.47% coverage

### Phase 2 — Server, CLIs and build probe resolve through the roster

- [x] `probe_build` returns `"b10537"` from the real captured stderr banner, `None` on an unparseable banner and on a stubbed subprocess failure, never raising; no real subprocess started — `src/wave_local_ai_v2/build_probe.py:23-47`; `tests/test_build_probe.py:15-70`
- [x] `server.build_flags` produces the exact validated flag list from the shipped entry plus host defaults 37/8; a dense entry or an over-ceiling MoE value raises `roster.RosterError` before any `subprocess.Popen` — `src/wave_local_ai_v2/server.py:39-95` (single `validate_host_fit` call site at `:55`); `tests/test_server.py:14-63,66-97` (asserts `mock_popen.assert_not_called()`)
- [x] A runtime row's `llama_cpp_build`/`model_file`/`quant`/`flags` trace to the probe, the roster and `build_flags`; no `LLAMA_CPP_BUILD`/`MODEL_RELATIVE_PATH`/`QUANT` constant in `__init__.py` — `src/wave_local_ai_v2/__init__.py:245,361-364`; grep over `src/` returns only the docstring mention at `build_probe.py:29`; `tests/test_cli.py:221-223,253-261`
- [x] A quality row's model path traces to the roster; no `MODEL_RELATIVE_PATH` in `quality_cli.py`; suite green — `src/wave_local_ai_v2/quality_cli.py:195-199,201-210`; quality rows carry no `quant` field in `REQUIRED_FIELDS["quality"]`, so that half is not-applicable

### Phase 3 — Byte-identical proof and documentation

- [x] `tests/test_launch_byte_identical.py` passes; the frozen list reads identically to `context_input/baseline_qwen36.md:8-10` field for field (verified line by line against the source document) — `tests/test_launch_byte_identical.py:32-64`
- [x] A real row exists with `llama_cpp_build == "b10537"`, `roster_entry_id == "qwen3.6-35b-a3b-ud-iq4xs"`, `roster_version == 1`, from a live invocation — `aidd_docs/results/runtime.jsonl`, `run_id=8199c549a2294ceb93d0babcff789fd2`, `captured_at=2026-08-23T07:07:07.266226+00:00` (the three earlier rows carry `roster_entry_id: null`, i.e. predate the change)
- [x] `CHANGELOG.md` gains an `### Added` bullet under `[Unreleased]`; `docs/setup.md`, `aidd_docs/memory/architecture.md` and `cli.md` describe the roster and the two host env vars instead of source constants — `CHANGELOG.md:12-18`, `docs/setup.md:90-99,164-177,213-223`, `architecture.md:46-53`, `cli.md:24-33`

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🟡 fixed | functional | 1 | `aidd_docs/roster/models.json:7`, `docs/setup.md:160,173-176` | Phase-1 criterion 1 asks the entry's five identity fields to match `docs/setup.md` step 3 verbatim. `file` does not: the roster holds a host-layout path (`Qwen3.6-35B-A3B/Qwen3.6-35B-A3B-UD-IQ4_XS.gguf`), the doc's `File:` bullet holds the repo-relative name (`Qwen3.6-35B-A3B-UD-IQ4_XS.gguf`). The new paragraph at `:173-176` states all four values "are the same ones the roster entry pins" and that any mismatch "is a bug, not a choice", so the doc now contradicts itself. | Reword `docs/setup.md:160` to name the field it actually mirrors (`File (repo-relative): …`, `Local path / roster \`file\`: Qwen3.6-35B-A3B/…`) and narrow the `:173-176` claim to repo/revision/checksum plus the code-block path; or split the roster field into a repo-relative `file` and a `local_subdir`, which also makes `https://huggingface.co/{repo}/resolve/{revision}/{file}` reconstructible from the entry. **Fixed**: the subdirectory layout is authoritative (it is what the `--local-dir` download step produces); `docs/setup.md:160-163` now lists the repo-relative name and the `SLM_MODELS_DIR`-relative path as separate bullets, `:165-169` says plainly that the weights are not flat, and `:172-178` claims verbatim agreement only for repo/revision/checksum plus that path. |
| 🟡 fixed | code | 1 | `src/wave_local_ai_v2/roster.py:107-145` | `_parse_entry` validates only top-level key presence and the `architecture` block. An entry whose `server_flags` is missing a key passes `load_roster`, then fails inside `server.build_flags` with a bare `KeyError` that neither `__init__.main()` (`:197-212`) nor `quality_cli.main()` (`:88-102`) catches — a traceback where every other roster failure is a one-line operator message. Reproduced: deleting `server_flags.context_size` from the shipped roster gives `load_roster passed the entry` then `build_flags raised: KeyError 'context_size'`. Contradicts the module docstring at `:4-5` ("refuses any entry missing a required field"). | Validate `server_flags` and its `sampler` sub-object in `_parse_entry` the same way `architecture` is validated at `:118-129` — required keys `{n_gpu_layers, context_size, flash_attention, jinja, parallel_slots, load_mode, sampler}` and `{temperature, top_p, top_k, min_p, presence_penalty}` — and type-check `roster_version` as `int` at `:104`, since a string version would otherwise land verbatim on every published row. **Fixed**: `roster.py:23-63` declares `REQUIRED_FIELDS` keyed by dotted block path (entry, `architecture`, `server_flags`, `server_flags.sampler`, `validated_host`), walked parents-first at `:186-190`; a missing field is now named by its full path (`server_flags.context_size`). `sha256` must be 64 lowercase hex (`:192-197`) and `roster_version` must be a non-`bool` int (`:131-138`). Regression covered at `tests/test_roster.py:215-230`, plus dotted-path, checksum, version and non-object-block cases at `:185-212,233-266`. |
| 🟡 fixed | fit | 3 | `tests/test_launch_byte_identical.py:73`, `tests/test_server.py:25`, `src/wave_local_ai_v2/settings.py:47-48` | The story's real claim is that the launched command **with default host values** is byte-identical. Both byte-identical tests pass the literals `host_n_cpu_moe=37, host_threads=8`, and `tests/test_roster.py:192-193` reads `entry.validated_host`; nothing reads `Settings().host_n_cpu_moe` or `load_settings()`. Editing the settings defaults leaves all 264 tests green while `uv run wave-local-ai-v2` launches a non-baseline command. `plan.md`'s Decisions table anticipated exactly this guard ("a test can assert the settings defaults equal `validated_host`") and it was not written. Verified today's defaults do produce the baseline command byte for byte — the property holds, it is just unguarded. | In `tests/test_launch_byte_identical.py`, build the flag list from `Settings(...)`'s `host_n_cpu_moe`/`host_threads` defaults rather than literals, and add an assertion that those defaults equal the shipped entry's `validated_host["n_cpu_moe"]`/`["threads"]`. **Fixed**: `settings.py:15-22` gives the two defaults a single definition (`DEFAULT_HOST_N_CPU_MOE`/`DEFAULT_HOST_THREADS`, used by both the dataclass and `load_settings`' fallback, which previously repeated the literals); `tests/test_launch_byte_identical.py:12-39,80-92` and `tests/test_server.py:23-31,34-46` now build the command from `Settings()`' defaults, and a new test asserts those defaults equal the shipped entry's `validated_host`. Proven: flipping the default 37→36 fails all three assertions. |
| 🟡 fixed | fit | 2 | `src/wave_local_ai_v2/quality_cli.py:41,142` | `LOCAL_MODEL_ID = "Qwen3.6-35B-A3B"` stays a literal and is published as every local quality row's `model_id`. `phase-2.md:96` instructed the opposite: "if kept, source it from the roster entry rather than a literal so it can't drift from the entry `_local_model_path` resolves." Set `ROSTER_ENTRY_ID` to a second entry and the rows publish the wrong model name while `roster_entry_id` names the right one — a contradiction inside one row, against the parent epic "every published row explains and reproduces itself". | Add a `display_id` field to the roster entry and read `model_id` from it (the roster is already the identity source), or derive it from the entry (`Path(entry.file).parent.name`, `entry.repo.rsplit("/", 1)[-1]`). Keep the comment explaining why it is not `entry.repo`. **Fixed**: `LOCAL_MODEL_ID` deleted; the roster entry carries a required `display_id` field (`roster.py:58-63`, `models.json:8`) and `quality_cli.py:134-137` reads the local row's `model_id` from the entry that actually launched. `tests/test_quality_cli.py:161-177` asserts it against a fixture `display_id` deliberately unlike the real one. |
| 🟡 fixed | conform | 3 | `.env.example` | The four new env vars (`SERVER_N_CPU_MOE`, `SERVER_THREADS`, `ROSTER_PATH`, `ROSTER_ENTRY_ID`) are documented in `docs/setup.md:213-223` but absent from the template, while every other overridable setting is listed there commented (`RUNTIME_REPETITIONS`, `RUNTIME_COOLDOWN_S`, `RUNTIME_WARMUP_COUNT`, `RUNTIME_SPREAD_THRESHOLD`). Step 4 walks the reader from `copy .env.example .env` straight into these vars, so the walkthrough and the template disagree. | Append the four as commented lines with their defaults, matching the `# RUNTIME_*` block's shape. **Fixed**: `.env.example:12-19` adds all four commented with today's defaults and a line each on when to override. |
| 🟢 filed | rot | 1 | `src/wave_local_ai_v2/roster.py:183-220` | `build_flags_from_entry` has no production caller: the flag ordering is written twice (here and `server.py:59-94`), and the second copy exists only for `tests/test_roster.py:184-217`'s strip-and-compare, which reconstructs by hand what `tests/test_launch_byte_identical.py` already asserts directly against the source document. | Delete `build_flags_from_entry` and replace `tests/test_roster.py:184-217` with a direct `server.build_flags` assertion, or give the helper a production caller by having `server.build_flags` compose it. |
| 🟢 filed | rot | 2 | `src/wave_local_ai_v2/classification_suite.py:48` | `CONTEXT_LENGTH = 32768` is now a bare literal duplicating the shipped entry's `server_flags.context_size` with nothing binding them, and the existing tech-debt row for this line still describes it as `CONTEXT_LENGTH = server.CONTEXT_SIZE`, a constant this diff deleted. | Update the stale tech-debt row's text; add a test asserting the literal equals the shipped entry's `context_size` so the two cannot drift silently across every published cloud row. |
| 🟢 filed | code | 2 | `tests/test_cli.py:34,220-222` | The fake entry's `file` is `"fake.gguf"` (no directory), so `row["model_file"] == FAKE_ROSTER[...]["file"]` passes whether `__init__.py:362` publishes `Path(entry.file).name` or the whole relative path — the one field where the roster's `file` shape differs from what the row publishes is unguarded. | Give the fake entry a subdirectory (`"fake-model/fake.gguf"`) and assert `row["model_file"] == "fake.gguf"`. |
| 🟢 filed | rot | 2 | `src/wave_local_ai_v2/quality_cli.py:47-63` | `LOCAL_SAMPLING`'s comments state the launched sampler values in prose ("server launched with `--temp 1.0`", "`--top-k 20`", "`--top-p 0.95`", "`--presence-penalty 1.5`"). Those are roster data now and go stale the first time a second entry is selected. | Reword to name the source ("the roster entry's `sampler` block, `--temp 1.0` for the shipped entry") rather than restating the values as fixed. |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 100% (10/10) after the in-branch fixes; 90% (9/10) as first reviewed |
| Files checked | `aidd_docs/roster/models.json`, `src/wave_local_ai_v2/roster.py`, `build_probe.py`, `server.py`, `settings.py`, `row_contract.py`, `__init__.py`, `quality_cli.py`, `classification_suite.py`, `tests/test_roster.py`, `test_build_probe.py`, `test_server.py`, `test_cli.py`, `test_quality_cli.py`, `test_row_contract.py`, `test_results.py`, `test_launch_byte_identical.py`, `CHANGELOG.md`, `docs/setup.md`, `aidd_docs/memory/architecture.md`, `aidd_docs/memory/cli.md`, `.secrets.baseline`, `.env.example` |
| Unchecked     | Phase 1 criterion 1 (roster `file` vs `docs/setup.md` step 3 verbatim) — fixed. Phase 2 criterion 4's "quant-bearing fields" half — not-applicable (quality rows carry no `quant`). |
| Unplanned     | `src/wave_local_ai_v2/classification_suite.py` (lost its `server.CONTEXT_SIZE` import as a consequence of phase 2's constant deletion; in no phase's architecture projection), `tests/test_results.py` and `tests/test_row_contract.py` row fixtures (implied by the contract change, named only in phase 1's task text), `.secrets.baseline` (the roster's sha256 allowlisted), `.env.example` (added by this review's fixes). All are justified consequences of planned work, none introduces behavior. |
